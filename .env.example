# Ticketmaster API key for backend routes.
TICKETMASTER_API_KEY=INSERT YOUR API KEY HERE

# Optional: comma-separated origins. Leave empty to allow all origins.
# CORS_ORIGINS=http://localhost:3000,https://your-domain.example

# Optional upstream timeout for Ticketmaster requests.
REQUEST_TIMEOUT_SECONDS=10
